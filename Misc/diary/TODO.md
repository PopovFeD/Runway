# TODO

## Документация (Misc/docs/)

- [x] transport.md
- [x] framing.md
- [x] protocol.md
- [x] storage.md
- [x] settings.md
- [x] viewmodels.md (MainWindowViewModel, ViewLocator, связь с GUI)
- [x] logging.md — телеметрийный лог (LogFileWriter/BoundedLog) vs
      diagnostics-лог (Microsoft.Extensions.Logging/FileLoggerProvider),
      почему их два и почему не Serilog/NLog
- [ ] README.md-индекс по Misc/docs/, когда наберётся достаточно файлов
- [ ] Чистка документации от старых, давно закрытых проблем, а также от того, что уже было протестировано вручную, но что не было задокументировано в логах

## Код — по итогам первого e2e (Misc/diary/2026.07.08.md)

Приоритет — перед подключением SQLite:

- [x] разделить read-поток и обработку через очередь
      (`System.Threading.Channels.Channel<Frame>`), иначе запись в БД
      будет тормозить чтение с порта
- [x] ограничить/очищать `LogEntries` (сейчас растёт бесконечно) —
      `BoundedLog` в GUI (капается по `MaxLogEntries`), полный лог
      всё равно пишется в файл (`LogFileWriter`, без ограничений)
- [x] переподключение при разрыве порта + индикация разрыва в GUI —
      `SerialTransport.RunLoop` переоткрывает порт с паузой
      (`ReconnectDelaySeconds`), `ConnectionStateChanged` прокинуто
      в `MainWindowViewModel.ConnectionStatus` и забиндено в GUI
- [x] забиндить выбор порта в `MainWindow.axaml` вместо жёсткого
      порта из `settings.json` — сделано шире исходной задачи: введён
      общий `ITransport` (Serial — рабочий, `WifiTransport` для ESP32 —
      заглушка), выбор транспорта и точки подключения в GUI, подключение
      по кнопке; `IPortLister`/`SerialPortLister` упразднены —
      перечисление точек подключения теперь обязанность самого транспорта
      (`GetAvailableEndpoints`)
- [x] слой хранения: SQLite (`ITelemetryStore`/`SqliteTelemetryStore`,
      Microsoft.Data.Sqlite 10.0.0 — версия проставлена без NuGet,
      проверить при restore); телеметрия пишется из ProcessFramesAsync
- [ ] `LogFileWriter` (`runway.log`) теперь дублирует телеметрию из БД —
      решить, когда БД станет основным хранилищем, не пора ли его убрать
- [x] `PacketParser.Parse` — типизированная иерархия `abstract record
      Packet` (`TelemetryPacket`/`EnvironmentPacket`/`ControlPacket`);
      заодно новый тип сообщения `Environment = 0x11` (давление+свет,
      uint32+uint32) сквозь эмулятор — демонстрация расширяемости под
      плитки Дашборда из макета
- [ ] Экспорт в Microsoft Excel
- [ ] Управляющие команды
- [ ] Оптимизация логов, сведение их в одну систему — РЕШЕНИЕ ПРИНЯТО,
      см. Misc/docs/storage-and-logs-decision.md (sessions/events в SQLite,
      diagnostics-файл как резерв, runway.log упраздняется); осталась
      реализация по шагам оттуда. Исходная заметка: Думается, все данные о работе приложения надо снести в одну систему: дублирование функций SQLite и .log файлов – не есть хорошо, надо смотреть, как такие коллизии разрешаются в реальных приложениях. Может, вводить отдельную галочку на подключение телеметрии, с полноценным логированием, а в SQL хранить... Всё равно непонятно, как действовать
- [x] Решение проблем, связанных с вызовом ошибок и предупреждений при тестах — NU1903 закрыт прямой ссылкой на SQLitePCLRaw.bundle_e_sqlite3 2.1.12 (проверить версию при restore)
- [x] Понять, почему один и тот же `dotnet test` может при запуске генерировать 2, 1 и 0 ошибок — разгадано: это интеграционные тесты при занятой паре com0com (эмулятор/приложение держат COM4/COM6); теперь они мягко скипаются, см. 2026.07.09-claude-code-2.md
- [ ] Увеличение покрытия тестами
- [ ] При смене конфигурации подключения (WiFi/Serial, какой порт) соответствующая запись обновляется в настройках `settings.json`. Сейчас, кажется, в этот момент не происходит вообще ничего. А стоило бы происходить. Добавление логики обновления при старте и сверки, существует ли вообще текущий способ подключения, логика для обработки случая отсутствия последнего способа подключения.
- [ ] Файл диагностики работает плохо. Он не должен срабатывать дважды, что он делает при подключении к "неудачному" порту, не должен выдавать 2 ошибки при подключении к занятому порту. Кажется, он работает от переключение выпадающего списка. Что нелогично: список ничего толком не делает, делают кнопки подключения и отключения.

Не срочно, но держать в уме:

- [ ] зафиксировать формат протокола (magic, коды типов) одним файлом,
      общим для Python и C#, чтобы не расходились молча повторно
      (см. баг с `TYPE_SENSOR`/magic из 2026.07.08.md)
- [ ] `FrameReader._buffer` на `List<byte>` — пересмотреть, если трафик
      станет высокочастотным (сейчас O(n) на `RemoveRange`)
- [ ] сам цикл переподключения в `SerialTransport` (открытие/переоткрытие
      реального `SerialPort`) не покрыт автотестами — только реакция
      `MainWindowViewModel` на уже случившееся событие. Если понадобится
      закрыть и это — придётся вводить абстракцию над `SerialPort`
      (`ISerialPortFactory` или похожую) ради фейка; com0com для этого
      не подходит (виртуальная пара портов не эмулирует физический разрыв)
- [ ] версии пакетов `Microsoft.Extensions.Logging`/`.Console` в
      `Runway.csproj` (`10.0.0`) проставлены по аналогии с
      `System.IO.Ports` не глядя в NuGet (сети не было) — проверить
      при первом `dotnet restore` и поправить, если предложит другую
- [ ] Функционал сброса настроек, отдельная вкладка. По аналогии с VS Code: считывание JSON, построение схемы UI и т.д.

## UI

- [ ] Фиксировать роль вывода в текстовом окне GUI как output последних не более чем n позиций или для последнего, к примеру, часа работы. Указывать на существование логов, потенциально – с просматриванием их прямо в GUI приложения в отдельной вкладке с применением фильтрации
- [ ] Облегчение тяжеловесности логов, переход к более VS-Code-овскому стилю вывода. Ну или в стиле Microsoft Powershell
- [ ] Переход ко стандартному layout "Слева направо сверху вниз", по иерархии вкладок, кнопок и основного окна. Ориентир – VS Code, Zen, Файловые менеджеры
- [ ] GUI для управляющих команд
- [ ] Обработка ошибок, в том числе – телеграфирование о них в GUI
- [ ] Адаптивность UI: Введение минимальных размеров окна и кнопок, перестройки. А то пока что при изменении размеров UI просто обрезается
- [ ] Кастомные иконки, самые простые. Может, использовать svg, лежащие у меня на компьютере, с трансляцией их в .ico
- [ ] Сопровождение В терминале последних записей, а не простаивание на месте при добавлении всё новых, а как следствие – более значимых записей
- [ ] Выбор протокола сообщения
- [ ] Кнопка отключения и подключения должна быть одной и той же, как я считаю

## Общие вопросы (перенесено из старого TODO)

- [ ] Dependency injection — стоит ли вводить контейнер, когда
      ручной DI в `App.axaml.cs` станет неудобным
