# TODO

Выполненные пункты не хранятся здесь — история решений в `Misc/diary/`
(по датам) и в `Misc/docs/` (архитектурные решения).

## Код

- [ ] Убрать создание runway.diagnostics: функционал стал бесполезен с новой системой отображения и экспорта во вкладке "Логи". Но при этом есть и альтернативное мнение, что SQLite в любой момент может быть повреждён, и лучше оставить этот способ ведения записей.
- [ ] Экспорт в логов .log, не в .csv
- [ ] Экспорт: CSV логов и CSV/XLSX телеметрии сделаны (вкладки «Логи»
      и «Экспорт», файлы в exports/); осталось: пока работает только для протокола v1, нужно решение для экспорта любого выбранного типа пакетов или их всех вместе.
- [ ] Сохранение выбора подключения в `settings.json` при Connect
      (`SettingsLoader.Save` есть) + сверка при старте, что сохранённый
      способ подключения ещё существует
- [ ] Двойные записи диагностики при неудачном подключении: Error из
      `TryOpenPort` + Warning из `RunLoop` на одну попытку — свести к одной. КАЖЕТСЯ, проблема решена
- [ ] Увеличение покрытия тестами
- [ ] Функционал сброса настроек, отдельная вкладка (считывание JSON,
      построение схемы UI — как в VS Code)

Не срочно, но держать в уме:

- [ ] зафиксировать формат протокола (magic, коды типов) одним файлом,
      общим для Python и C# — синхронизация пока ручная, уже дважды
      стреляла (см. 2026.07.08.md)
- [ ] `FrameReader._buffer` на `List<byte>` — пересмотреть при
      высокочастотном трафике (O(n) на `RemoveRange`)
- [ ] цикл переподключения `SerialTransport` не покрыт автотестами —
      потребует `ISerialPortFactory`-абстракцию ради фейка; com0com
      физический разрыв не эмулирует
- [ ] WiFi-транспорт для ESP32: заглушка `WifiTransport` → реальный
      TCP-клиент + обнаружение устройств (mDNS/список в настройках)
- [ ] Dependency injection — стоит ли вводить контейнер, когда ручной
      DI в `App.axaml.cs` станет неудобным
- [ ] Управляющие команды (устройству): `PacketBuilder` готов,
      нужен `MessageType.Command` + отправка в `ITransport` (метод Write
      в интерфейсе отсутствует — добавить) + GUI на вкладке «Команды»

## UI

- [ ] "Экспорт": убрать текст "Всего в БД: 77 записей"
- [ ] Просмотр телеметрии из БД (история, не только события) + фильтры
- [ ] GUI для управляющих команд (вкладка-заглушка уже есть)
- [ ] Телеграфирование ошибок в GUI (не только в логи)
- [ ] Адаптивность: перестройка плиток на узком окне (MinWidth/MinHeight
      уже стоят)
- [ ] Выбор протокола сообщения
- [ ] Улучшение окна подключения: параметры бод при подключении, задержки переподключения
- [ ] Документация Misc/docs: README-индекс; чистка от давно закрытых
      проблем
